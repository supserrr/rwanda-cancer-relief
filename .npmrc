# Allow build scripts for specific packages
# These packages require build scripts to function correctly
# Using enable-pre-post-scripts=true allows all packages to run build scripts
# This is safe for these legitimate packages that need build scripts
enable-pre-post-scripts=true

