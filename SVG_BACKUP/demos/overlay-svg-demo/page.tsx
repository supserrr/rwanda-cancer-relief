import { Skiper19 } from "@/components/ui/svg-follow-scroll";

/**
 * Overlay SVG Demo page
 * 
 * This page showcases the original SVG follow scroll component
 * with the separated sections overlaid on top of the SVG animation.
 * The SVG animation functions exactly as before, but now has
 * additional content layered on top.
 * 
 * @returns {JSX.Element} The overlay SVG demo page
 */
export default function OverlaySvgDemo() {
  return <Skiper19 />;
}
