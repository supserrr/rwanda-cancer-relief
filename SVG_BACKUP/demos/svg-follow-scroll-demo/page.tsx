import { Skiper19 } from "@/components/ui/svg-follow-scroll";

/**
 * Demo page showcasing the original SVG Follow Scroll component design.
 * 
 * This page demonstrates the exact scroll-linked SVG animation effect
 * from the original design where an SVG path progressively draws as the user scrolls.
 * 
 * @returns A full-page demo with the original scroll-animated SVG design
 */
export default function SvgFollowScrollDemo() {
  return <Skiper19 />;
}
