'use client';

import { Skiper19 } from '@/components/ui/svg-follow-scroll';

/**
 * Demo page showcasing the SVG Follow Scroll component.
 * 
 * This page demonstrates the original scroll-linked SVG animation effect
 * where an SVG path progressively draws as the user scrolls.
 * 
 * @returns A full-page demo with scroll-animated SVG
 */
export default function SvgScrollDemo() {
  return <Skiper19 />;
}

