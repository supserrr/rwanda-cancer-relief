'use client';

import { SvgFollowScroll } from '@workspace/ui/components/ui/svg-follow-scroll';

/**
 * Demo page showcasing the SvgFollowScroll component.
 * 
 * This page demonstrates the scroll-linked SVG animation effect
 * where an SVG path progressively draws as the user scrolls.
 * 
 * @returns A full-page demo with scroll-animated SVG
 */
export default function SvgScrollDemo() {
  return <SvgFollowScroll />;
}

