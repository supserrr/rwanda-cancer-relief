# Server Configuration
# Port changed from 5000 to 10000 to avoid macOS AirPlay Receiver conflict
PORT=10000
NODE_ENV=development

# Supabase Configuration (Required)
# Get these from: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... # anon public key
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... # service_role secret key (NEVER expose)

# Jitsi Meet Configuration (Required for video calls)
# Get from: https://jitsi.org/
JITSI_APP_ID=your_jitsi_app_id
JITSI_APP_SECRET=your_jitsi_app_secret
JITSI_DOMAIN=8x8.vc

# Frontend Configuration (Required)
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# JWT Configuration (Required if using custom JWT alongside Supabase)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# Email/Notifications (Optional - choose one method)
# Option 1: Resend (Recommended)
# Get from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# Option 2: SMTP (Alternative)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# SMTP_FROM=noreply@yourdomain.com

# Render Configuration (Automatically set by Render - no need to set manually)
# RENDER_EXTERNAL_URL=https://your-app.onrender.com
# RENDER_SERVICE_NAME=your-service-name
# RENDER_GIT_BRANCH=main
# RENDER_GIT_COMMIT=abc123...

# Supabase OAuth (Optional - only if building Supabase integrations)
# SUPABASE_CLIENT_ID=your_oauth_client_id
# SUPABASE_CLIENT_SECRET=your_oauth_client_secret
