'use client';

import { ParallaxScrollFeatureSection } from '@workspace/ui/components/ui/parallax-scroll-feature-section';

/**
 * Demo page showcasing the ParallaxScrollFeatureSection component.
 * 
 * This page demonstrates the parallax scroll effect with default content
 * and animations that trigger as the user scrolls through the sections.
 * 
 * @returns A full-page demo with parallax scroll animations
 */
export default function ParallaxDemo() {
  return <ParallaxScrollFeatureSection />;
}

